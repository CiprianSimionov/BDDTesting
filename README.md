**BDD design aproach** - Created with Selenium and feature files using Gherkin syntax.

* Run tests using:
`behave`
* Install:
`behave-html-formatter`
* Generate reports:
`behave -f behave_html_formatter:HTMLFormatter -o name-of-the-report.html`

